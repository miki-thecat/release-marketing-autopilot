// Adapted from Tailark OSS Dusk Call to Action 2, installed with shadcn CLI.
import Link from "next/link"; import { ArrowRight } from "lucide-react"; import { Button } from "@/components/ui/button"; import { marketingContent } from "@/content/marketing";
export default function CallToAction() { const { finalCta } = marketingContent; return <section className="py-16 md:py-20"><div className="mx-auto flex max-w-7xl items-end justify-between gap-6 px-6 max-lg:flex-col max-lg:items-center max-lg:text-center"><div><p className="text-sm text-muted-foreground">{finalCta.eyebrow}</p><h2 className="mt-4 max-w-4xl text-balance text-5xl font-semibold tracking-tight xl:text-6xl">{finalCta.title}</h2><p className="mt-4 text-sm text-muted-foreground">{finalCta.note}</p></div><Button size="lg" nativeButton={false} render={<Link href="/create">{finalCta.action}<ArrowRight /></Link>} /></div></section>; }
