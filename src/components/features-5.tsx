// Adapted from Tailark OSS Dusk Features 5, installed with shadcn CLI.
import { Check, Play } from "lucide-react";
import { Card } from "@/components/ui/card";

const stages = [["Create", "Give the release a name, context, and browser recording."], ["Plan", "Validate media, analyze it, and plan the story."], ["Processing", "See job progress through the release workflow."], ["Render", "Render a focused 1080p H.264 release video."], ["Release Pack", "Preview the video and copy English X and LinkedIn posts."]] as const;
export default function FeaturesFive() { return <section className="py-16 md:py-20"><div className="mx-auto max-w-7xl px-6"><h2 className="max-w-4xl text-balance text-4xl font-medium tracking-tight text-muted-foreground"><span className="text-foreground">Built for the full release workflow.</span><br />The product proof follows the real route, not a generic campaign.</h2><div className="mt-16 grid gap-6 lg:grid-cols-[12rem_1fr]"><aside className="sticky top-24 h-fit max-lg:hidden"><p className="text-sm text-muted-foreground">Product</p><ol className="mt-4 space-y-1">{stages.map(([label]) => <li key={label}><a href={`#stage-${label.toLowerCase().replace(" ", "-")}`} className="block rounded-full px-4 py-2 text-sm text-muted-foreground hover:bg-muted hover:text-foreground">{label}</a></li>)}</ol></aside><div className="flex flex-col gap-16 md:gap-28">{stages.map(([label, description], index) => <article id={`stage-${label.toLowerCase().replace(" ", "-")}`} key={label} className="grid scroll-mt-32 gap-6 sm:grid-cols-2 md:grid-cols-5"><div className="md:col-span-2"><h3 className="text-sm font-medium text-muted-foreground">{label}</h3><p className="mt-6 text-lg font-medium">{description}</p><p className="mt-8 text-sm text-muted-foreground">Current ReleaseFlow interface · representative state</p></div><Card className="relative grid aspect-square place-items-center overflow-hidden p-6 md:col-span-3">{index === 3 ? <Play className="size-8 fill-current" /> : <div className="w-full space-y-3"><p className="border-b border-border pb-3 font-medium">{label}</p>{["Recording", "Story", "Output"].map((item, itemIndex) => <div className="flex items-center gap-3 rounded-lg border border-border p-3 text-sm" key={item}><span className="grid size-5 place-items-center rounded-full bg-foreground text-[9px] text-background">{itemIndex + 1}</span>{item}{index === 4 && itemIndex === 2 ? <Check className="ml-auto size-4" /> : null}</div>)}</div>}</Card></article>)}</div></div></div></section>; }
